/*
import 'package:geolocation/geolocation.dart';

class MyLocation {
  LocationResult actuLocation;
  //
  double lng;
  double lat;

  MyLocation() {
    getLocation();
  }

  getLocation() async {
    actuLocation = await Geolocation.lastKnownLocation();
    this.lat = actuLocation.location.latitude;
    this.lng = actuLocation.location.longitude;
  }

  double get longitude {
    return this.lng;
  }

  double get latitude {
    return this.lat;
  }
}
*/